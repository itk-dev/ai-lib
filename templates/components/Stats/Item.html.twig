{% if app.environment == 'dev' %}<!-- {{ _self }} -->{% endif %}
{% props label, value %}
<div class="flex flex-col gap-1">
    <dt class="text-xs uppercase tracking-widest text-text-muted">{{ label }}</dt>
    <dd class="font-display text-3xl font-semibold text-ink">{{ value }}</dd>
</div>
{% if app.environment == 'dev' %}<!-- /{{ _self }} -->{% endif %}
